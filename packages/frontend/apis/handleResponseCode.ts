// import { useMessage } from "components/Message"

// const useMess = useMessage()

export function handleResponseCode(code: number, msg: string) {
  if (code === 0) {
    return true;
  }
  // useMess('error', msg)
}
